const woodConfig = require('@wood/config/subscriptions');

/**
 * @type {Object} Application subscriptions config values.
 */
module.exports = {
  /**
   * Start with default subscriptions config.
   */
  ...woodConfig,

  // Overwrite default configs here
};
