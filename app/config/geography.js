const woodConfig = require('@wood/config/geography');

/**
 * @type {Object} Application geography config values.
 */
module.exports = {
  /**
   * Start with default geography config.
   */
  ...woodConfig,

  // Overwrite default configs here
};
